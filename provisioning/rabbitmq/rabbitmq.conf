# RabbitMQ Configuration for Bannou

# Allow guest user to connect from any host (required for Docker)
loopback_users = none

# Load exchange and policy definitions on startup
load_definitions = /etc/rabbitmq/definitions.json
