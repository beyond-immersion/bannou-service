apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: permissions-store
spec:
  type: state.redis
  version: v1
  metadata:
  - name: redisHost
    value: auth-redis:6379  # Use auth-redis for permissions in CI
  - name: redisPassword
    value: ""
