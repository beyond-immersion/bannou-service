# Service Dependencies - WSL2 Compatible
# Database and caching services using default bridge network
# All services reachable by service name (bannou-redis, account-db)

services:

  # Redis Stack for state/caching with RediSearch (FT.* commands)
  # Uses redis-stack-server which includes RediSearch, RedisJSON, RedisBloom, RedisTimeSeries
  bannou-redis:
    image: redis/redis-stack-server:latest
    ports:
      - "6379:6379"  # For development access
    command: redis-server --databases 16 --appendonly yes --loadmodule /opt/redis-stack/lib/redisearch.so
    volumes:
      - bannou-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # MySQL for accounts service
  account-db:
    image: mysql:8.1.0
    environment:
      - MYSQL_DATABASE=accounts
      - MYSQL_USER=guest
      - MYSQL_PASSWORD=guest
      - MYSQL_ROOT_PASSWORD=rootpassword
    volumes:
      - account-data:/var/lib/mysql
      - ./mysql/my.cnf:/etc/mysql/conf.d/my.cnf
      - ./mysql/run-init/:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "guest", "-pguest"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  bannou-redis-data:
  account-data:
