# Ingress Layer - OpenResty Edge Proxy
# OpenResty on dual networks: default (internal backend) + external (client-facing)
# This enables edge tests to simulate real external clients

services:

  bannou:
    labels:
      - "openresty.enable=true"
      - "openresty.backend.auth=/authorization"
      - "openresty.backend.connect=/connect"

  # OpenResty Edge Proxy with Lua + Redis routing
  openresty:
    image: openresty/openresty:1.21.4.2-0-alpine
    # No ports in base - external.yml adds 80/443 for production
    volumes:
      - ./openresty/nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
      - ./openresty/lua:/usr/local/openresty/lua:ro
      - ./openresty/generated:/usr/local/openresty/generated:ro
      - ./openresty/overrides:/usr/local/openresty/overrides:ro
      - ./certificates:/certs:ro
    environment:
      # Use bannou-redis for service routing (orchestrator writes routing data there)
      - REDIS_HOST=bannou-redis
      - REDIS_PORT=6379
      # Asset state Redis (same as bannou-redis) for token validation
      - ASSET_REDIS_HOST=bannou-redis
      - ASSET_REDIS_PORT=6379
      - BANNOU_SERVICE_DOMAIN=${BANNOU_SERVICE_DOMAIN}
    networks:
      - default   # Internal network - reach bannou, routing-redis via service names
      - external  # External network - accept edge tester connections
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:80/health"]
      interval: 30s
      timeout: 3s
      retries: 5
      start_period: 60s

  # Redis for OpenResty routing state and service heartbeats
  routing-redis:
    image: redis:latest
    command: redis-server --appendonly yes
    volumes:
      - routing-redis-data:/data
    # No ports exposed - accessed via Docker network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      timeout: 3s
      retries: 5
      interval: 5s

# External network for edge testing (simulates client outside datacenter)
# OpenResty bridges internal (default) and external networks
networks:
  external:
    driver: bridge

volumes:
  routing-redis-data:
