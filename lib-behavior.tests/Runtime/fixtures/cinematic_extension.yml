# Test fixture: Cinematic extension
# Replaces default flow when injected at continuation point
version: "2.0"
metadata:
  id: test-cinematic-extension

flows:
  main:
    actions:
    - global: { variable: after_cp, value: "99.0" }
