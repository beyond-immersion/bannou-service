openapi: 3.0.3
info:
  title: Behavior Service Configuration
  description: "Configuration schema for Behavior service.\nProperties are bound to environment variables with BANNOU_ prefix."
  version: 1.0.0
paths: {}
x-service-configuration:
  properties:
    Enabled:
      type: boolean
      description: Enable/disable Behavior service
      default: true
