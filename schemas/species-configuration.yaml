openapi: 3.0.3
info:
  title: Species Service Configuration
  description: |
    Configuration schema for Species service.
    Properties are bound to environment variables with BANNOU_ prefix.

    **NRT Compliance**: AI agents MUST review docs/NRT-SCHEMA-RULES.md before
    modifying this schema. Optional reference types require `nullable: true`.
  version: 1.0.0
paths: {}
x-service-configuration:
  properties:
    # Seed Operation Configuration
    SeedPageSize:
      type: integer
      description: Number of records to process per page during seed operations
      env: SPECIES_SEED_PAGE_SIZE
      default: 100
