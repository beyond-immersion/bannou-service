openapi: 3.0.3
info:
  title: Location Service Configuration
  description: |
    Configuration schema for Location service.
    Properties are bound to environment variables with BANNOU_ prefix.

    **NRT Compliance**: AI agents MUST review docs/NRT-SCHEMA-RULES.md before
    modifying this schema. Optional reference types require `nullable: true`.
  version: 1.0.0
paths: {}
x-service-configuration:
  properties:
    Enabled:
      type: boolean
      description: Enable/disable Location service
      default: true
      env: LOCATION_ENABLED
