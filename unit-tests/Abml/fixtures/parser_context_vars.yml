version: "2.0"
metadata:
  id: context_test
context:
  variables:
    counter:
      type: int
      default: 0
    name:
      type: string
      source: "${entity.name}"
flows:
  start:
    actions: []
