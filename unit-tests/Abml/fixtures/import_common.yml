version: "2.0"
metadata:
  id: import_common_test

flows:
  greet:
    actions:
      - log: { message: "Hello from common!" }

  farewell:
    actions:
      - log: { message: "Goodbye from common!" }
