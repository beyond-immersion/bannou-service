version: "2.0"
metadata:
  id: increment
flows:
  start:
    actions:
      - set:
          variable: counter
          value: "5"
      - increment:
          variable: counter
          by: 3
      - log: { message: "Counter: ${counter}" }
