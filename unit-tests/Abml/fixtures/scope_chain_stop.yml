version: "2.0"
metadata:
  id: chain_stop
on_error: doc_handler
flows:
  start:
    actions:
      - goto: nowhere
    on_error:
      - log: "Flow handled"
  doc_handler:
    actions:
      - log: "Document handled"
