version: "2.0"
metadata:
  id: main

imports:
  - file: "lib.yml"
    as: "lib"

flows:
  start:
    actions:
      - cond:
          - when: "${flag}"
            then:
              - call: { flow: "lib.yes" }
          - else:
              - call: { flow: "lib.no" }
