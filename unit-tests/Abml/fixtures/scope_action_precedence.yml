version: "2.0"
metadata:
  id: action_precedence
flows:
  start:
    actions:
      - failing_action:
          param: value
          on_error:
            - log: "Action handled"
    on_error:
      - log: "Flow handled"
