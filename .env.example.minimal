# =============================================================================
# BANNOU PRODUCTION CONFIGURATION (Minimal)
# =============================================================================
# This is a minimal configuration for production deployment.
# All services run in monoservice mode (SERVICES_ENABLED=true).
#
# Generate secrets with: openssl rand -hex 32 (for 64-char) or -hex 16 (for 32-char)
# =============================================================================

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================
# Primary domain for the service. When set, OAuth redirect URIs and WebSocket
# URLs automatically default to this domain (no need to set individual URLs).
BANNOU_SERVICE_DOMAIN=your-domain.example.com

# =============================================================================
# SECRETS (Required - generate unique values for each environment)
# =============================================================================
# JWT secret for token signing/validation (64 characters recommended)
# Generate with: openssl rand -hex 32
BANNOU_JWT_SECRET=

# Server salt for client GUID generation (32 characters)
# Must be shared across all Connect instances
# Generate with: openssl rand -hex 16
CONNECT_SERVER_SALT=

# Internal service authentication token (64 characters)
# Generate with: openssl rand -hex 32
CONNECT_INTERNAL_SERVICE_TOKEN=

# =============================================================================
# DATABASE CREDENTIALS
# =============================================================================
MYSQL_USER=bannou_prod
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=
STATE_MYSQL_CONNECTION_STRING=server=account-db;database=accounts;user=bannou_prod;password=YOUR_MYSQL_PASSWORD

# =============================================================================
# OPTIONAL URL OVERRIDES
# =============================================================================
# These are auto-derived from BANNOU_SERVICE_DOMAIN if not set:
# - AUTH_CONNECT_URL defaults to: wss://{BANNOU_SERVICE_DOMAIN}/connect
# - CONNECT_URL defaults to: wss://{BANNOU_SERVICE_DOMAIN}/connect
# - OAuth redirect URIs default to: https://{BANNOU_SERVICE_DOMAIN}/auth/oauth/{provider}/callback
#
# Uncomment and set only if you need non-standard URLs:
# AUTH_CONNECT_URL=wss://your-domain.example.com/connect
# CONNECT_URL=wss://your-domain.example.com/connect
# AUTH_PASSWORD_RESET_BASE_URL=https://your-domain.example.com/reset-password

# =============================================================================
# ADMIN CONFIGURATION
# =============================================================================
# Comma-separated list of emails that get admin role on registration
ACCOUNT_ADMIN_EMAILS=admin@example.com
# Email domain suffix for automatic admin role (optional)
# ACCOUNT_ADMIN_EMAIL_DOMAIN=@yourcompany.com

# =============================================================================
# DOCKER NETWORKING
# =============================================================================
# Internal hostname for service mesh routing (should match docker service name)
MESH_ENDPOINT_HOST=bannou

# =============================================================================
# SERVICE MODE
# =============================================================================
# Enable all services in monoservice mode
SERVICES_ENABLED=true
