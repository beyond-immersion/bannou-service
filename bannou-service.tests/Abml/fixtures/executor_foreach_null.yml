version: "2.0"
metadata:
  id: foreach_null
flows:
  start:
    actions:
      - for_each:
          variable: item
          collection: "${items}"
          do:
            - log: { message: "Item: ${item}" }
      - log: "Done"
