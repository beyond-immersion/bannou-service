version: "2.0"
metadata:
  id: main

imports:
  - file: "lib.yml"
    as: "lib"

flows:
  start:
    actions:
      - for_each:
          variable: item
          collection: "${items}"
          do:
            - call: { flow: "lib.process" }
