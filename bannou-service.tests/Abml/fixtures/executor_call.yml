version: "2.0"
metadata:
  id: call_test
flows:
  start:
    actions:
      - log: "Before call"
      - call: { flow: subroutine }
      - log: "After call"
  subroutine:
    actions:
      - log: "In subroutine"
