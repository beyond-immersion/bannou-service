version: "2.0"
metadata:
  id: scope_read_main

imports:
  - file: "scope_read_lib.yml"
    as: "lib"

flows:
  start:
    actions:
    - set:
        variable: greeting
        value: "Hello from main"
    - call: { flow: "lib.print_greeting" }
