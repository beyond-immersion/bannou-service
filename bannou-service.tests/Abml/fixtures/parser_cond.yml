version: "2.0"
metadata:
  id: cond_test
flows:
  start:
    actions:
      - cond:
          - when: "${x > 10}"
            then:
              - log: "High"
          - when: "${x > 5}"
            then:
              - log: "Medium"
          - else:
              - log: "Low"
