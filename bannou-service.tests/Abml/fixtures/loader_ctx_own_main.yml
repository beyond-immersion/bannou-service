version: "2.0"
metadata:
  id: ctx_own_main

imports:
  - file: "ctx_own_lib.yml"
    as: "lib"

flows:
  start:
    actions:
    - log: { message: "Calling lib" }
    - call: { flow: "lib.show_config" }
