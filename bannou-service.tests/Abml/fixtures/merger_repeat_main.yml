version: "2.0"
metadata:
  id: main

imports:
  - file: "lib.yml"
    as: "lib"

flows:
  start:
    actions:
      - repeat:
          times: 3
          do:
            - call: { flow: "lib.tick" }
