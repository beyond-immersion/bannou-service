version: "2.0"
metadata:
  id: cond_true
flows:
  start:
    actions:
      - set:
          variable: x
          value: "10"
      - cond:
          - when: "${x > 5}"
            then:
              - log: "High"
          - else:
              - log: "Low"
