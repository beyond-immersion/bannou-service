version: "2.0"
metadata:
  id: schema_import_main

imports:
  - schema: "types.json"
    types: ["MyCustomType"]
  - file: "real_import.yml"
    as: "real"

flows:
  start:
    actions:
    - log: { message: "Hello" }
