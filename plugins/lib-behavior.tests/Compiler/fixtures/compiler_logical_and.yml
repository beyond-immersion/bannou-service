version: "2.0"
metadata:
  id: test

context:
  variables:
    a: { type: bool, default: true }
    b: { type: bool, default: true }

flows:
  main:
    actions:
      - cond:
          - when: "${a && b}"
            then:
              - log: { message: "Both true" }
