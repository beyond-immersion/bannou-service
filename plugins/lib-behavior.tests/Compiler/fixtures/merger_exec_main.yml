version: "2.0"
metadata:
  id: main

imports:
  - file: "lib.yml"
    as: "lib"

flows:
  start:
    actions:
      - log: { message: "Start" }
      - call: { flow: "lib.greet" }
      - log: { message: "End" }
