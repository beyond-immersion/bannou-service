<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <OutputType>Exe</OutputType>
    <IsTestProject>true</IsTestProject>
    <RootNamespace>BeyondImmersion.BannouService.CharacterPersonality.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Build tools (xunit comes from test-utilities; Test.Sdk and Moq needed directly) -->
    <PackageReference Include="xunit.v3" Version="3.2.2" />
    <PackageReference Include="Moq" Version="4.20.72" />
    <!-- Logging.Abstractions, Mvc.Core come from lib-character-personality → bannou-service -->
  </ItemGroup>

  <ItemGroup>
    <!-- bannou-service comes transitively via lib-character-personality -->
    <ProjectReference Include="../lib-character-personality/lib-character-personality.csproj" />
    <!-- lib-messaging for PublishOptions and IMessageBus testing -->
    <ProjectReference Include="../lib-messaging/lib-messaging.csproj" />
    <!-- lib-state for IStateStoreFactory and IStateStore testing -->
    <ProjectReference Include="../lib-state/lib-state.csproj" />
    <!-- Shared test utilities (ServiceConstructorValidator, etc.) -->
    <ProjectReference Include="../../test-utilities/test-utilities.csproj" />
  </ItemGroup>

</Project>
