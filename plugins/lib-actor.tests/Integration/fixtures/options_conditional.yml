version: "2.0"
metadata:
  id: conditional_available_test
  type: behavior

options:
  combat:
    - actionId: sword_slash
      preference: 0.8
      available: "${equipment.has_sword}"
      requirements:
        - has_sword

flows:
  main:
    actions:
      - log: "tick"
