version: "2.0"
metadata:
  id: error_handling_test
  type: behavior

options:
  combat:
    - actionId: broken_option
      preference: "${throw_error}"
      available: true
    - actionId: valid_option
      preference: 0.5
      available: true

flows:
  main:
    actions:
      - log: "tick"
