<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="../ServiceLib.targets" />

  <PropertyGroup>
    <!-- Extends base RootNamespace from ServiceLib.targets -->
    <RootNamespace>$(RootNamespace).Mesh</RootNamespace>
    <ServiceLib>mesh</ServiceLib>
  </PropertyGroup>

  <ItemGroup>
    <!-- Only packages NOT provided by bannou-service - NSwag, etc. come transitively -->
  </ItemGroup>

  <ItemGroup>
    <!-- Lua scripts for distributed circuit breaker atomic operations -->
    <EmbeddedResource Include="Scripts\*.lua" />
  </ItemGroup>

</Project>
