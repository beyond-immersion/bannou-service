// <auto-generated>
// BannouEvents.h - Client event types for Bannou WebSocket push events
// Generated by generate-unreal-types.py on 2026-03-01 19:16:34 UTC
// Do not edit this file manually.
// </auto-generated>

#pragma once

#include "CoreMinimal.h"

/**
 * Bannou Client Events
 *
 * This header contains event name constants for server-to-client push events.
 * These events are received via WebSocket when subscribed.
 */

namespace Bannou
{
    namespace Events
    {
        /** Sent when bundle creation from asset_ids completes. */
        constexpr const TCHAR* AssetBundleCreationComplete = TEXT("asset.bundle.creation.complete");

        /** Sent when a bundle upload has been validated and processed. */
        constexpr const TCHAR* AssetBundleValidationComplete = TEXT("asset.bundle.validation.complete");

        /** Sent when bundle validation fails. Includes detailed error information. */
        constexpr const TCHAR* AssetBundleValidationFailed = TEXT("asset.bundle.validation.failed");

        /** Sent when async metabundle creation job completes (success or failure). */
        constexpr const TCHAR* AssetMetabundleCreationComplete = TEXT("asset.metabundle.creation.complete");

        /** Sent when asset processing (e.g., texture mipmaps, model LODs) completes. */
        constexpr const TCHAR* AssetProcessingComplete = TEXT("asset.processing.complete");

        /** Sent when asset processing fails. Includes retry information. */
        constexpr const TCHAR* AssetProcessingFailed = TEXT("asset.processing.failed");

        /** Final notification that an asset is ready for use. */
        constexpr const TCHAR* AssetReady = TEXT("asset.ready");

        /** Sent when an asset upload has completed (success or failure). */
        constexpr const TCHAR* AssetUploadComplete = TEXT("asset.upload.complete");

        /** Sent to all existing sessions when a new login occurs on the account. */
        constexpr const TCHAR* AuthDeviceLogin = TEXT("auth.device-login");

        /** Sent to all sessions when a new OAuth provider is linked to the account. */
        constexpr const TCHAR* AuthExternalAccountLinked = TEXT("auth.external-account-linked");

        /** Sent to all sessions when MFA is disabled on the account. */
        constexpr const TCHAR* AuthMfaDisabled = TEXT("auth.mfa-disabled");

        /** Sent to all sessions when MFA is enabled on the account. */
        constexpr const TCHAR* AuthMfaEnabled = TEXT("auth.mfa-enabled");

        /** Sent to all sessions when the account's password is reset. */
        constexpr const TCHAR* AuthPasswordChanged = TEXT("auth.password-changed");

        /** Sent to remaining sessions when a session is remotely terminated. */
        constexpr const TCHAR* AuthSessionTerminated = TEXT("auth.session-terminated");

        /** Sent to all sessions when a failed login attempt occurs with a known account. */
        constexpr const TCHAR* AuthSuspiciousLogin = TEXT("auth.suspicious-login");

        /** Published when a character is transferred to a different realm. */
        constexpr const TCHAR* CharacterRealmTransferred = TEXT("character.realm-transferred");

        /** Published when a character's state changes (death, name change, status */
        constexpr const TCHAR* CharacterUpdated = TEXT("character.updated");

        /** Sent to room participants when a message is deleted. */
        constexpr const TCHAR* ChatMessageDeleted = TEXT("chat.message.deleted");

        /** Sent to room participants when a message is pinned or unpinned. */
        constexpr const TCHAR* ChatMessagePinned = TEXT("chat.message.pinned");

        /** Sent to room participants when a new message is received. */
        constexpr const TCHAR* ChatMessageReceived = TEXT("chat.message.received");

        /** Sent to all room participants when someone is banned. */
        constexpr const TCHAR* ChatParticipantBanned = TEXT("chat.participant.banned");

        /** Sent to room participants when someone joins the room. */
        constexpr const TCHAR* ChatParticipantJoined = TEXT("chat.participant.joined");

        /** Sent to all room participants when someone is kicked. */
        constexpr const TCHAR* ChatParticipantKicked = TEXT("chat.participant.kicked");

        /** Sent to room participants when someone leaves the room. */
        constexpr const TCHAR* ChatParticipantLeft = TEXT("chat.participant.left");

        /** Sent to all room participants when someone is muted. */
        constexpr const TCHAR* ChatParticipantMuted = TEXT("chat.participant.muted");

        /** Sent to room participants when a participant's role changes. */
        constexpr const TCHAR* ChatParticipantRoleChanged = TEXT("chat.participant.role-changed");

        /** Sent to room participants when a ban is lifted. */
        constexpr const TCHAR* ChatParticipantUnbanned = TEXT("chat.participant.unbanned");

        /** Sent to all room participants when someone is unmuted. */
        constexpr const TCHAR* ChatParticipantUnmuted = TEXT("chat.participant.unmuted");

        /** Sent to room participants when the room is being deleted. */
        constexpr const TCHAR* ChatRoomDeleted = TEXT("chat.room.deleted");

        /** Sent to room participants when the room is locked. */
        constexpr const TCHAR* ChatRoomLocked = TEXT("chat.room.locked");

        /** Sent to room participants when room settings change. */
        constexpr const TCHAR* ChatRoomUpdated = TEXT("chat.room.updated");

        /** Sent to room participants when someone starts typing. */
        constexpr const TCHAR* ChatTypingStarted = TEXT("chat.typing-started");

        /** Sent to room participants when someone stops typing. */
        constexpr const TCHAR* ChatTypingStopped = TEXT("chat.typing-stopped");

        /** Sent to the collection owner when progressive discovery advances for an entry. */
        constexpr const TCHAR* CollectionDiscoveryAdvanced = TEXT("collection.discovery.advanced");

        /** Sent to the collection owner when an entry is unlocked in their collection. */
        constexpr const TCHAR* CollectionEntryUnlocked = TEXT("collection.entry.unlocked");

        /** Sent to the collection owner when a completion milestone is crossed. */
        constexpr const TCHAR* CollectionMilestoneReached = TEXT("collection.milestone-reached");

        /** Sent to client when their available API capabilities change. */
        constexpr const TCHAR* ConnectCapabilityManifest = TEXT("connect.capability-manifest");

        /** Sent to client before WebSocket connection is closed. */
        constexpr const TCHAR* ConnectDisconnectNotification = TEXT("connect.disconnect-notification");

        /** Sent to the wallet owner when any balance mutation occurs: credits, debits, */
        constexpr const TCHAR* CurrencyBalanceChanged = TEXT("currency.balance.changed");

        /** Sent to the wallet owner when their wallet is frozen (escrow dispute, */
        constexpr const TCHAR* CurrencyWalletFrozen = TEXT("currency.wallet.frozen");

        /** Sent to the wallet owner when their wallet is unfrozen and usable again. */
        constexpr const TCHAR* CurrencyWalletUnfrozen = TEXT("currency.wallet.unfrozen");

        /** Sent to relevant players when a game action produces results. */
        constexpr const TCHAR* GameSessionActionResult = TEXT("game-session.action-result");

        /** Sent to recipients when a chat message is posted in the session. */
        constexpr const TCHAR* GameSessionChatReceived = TEXT("game-session.chat-received");

        /** Sent to all session participants when a new player joins. */
        constexpr const TCHAR* GameSessionPlayerJoined = TEXT("game-session.player-joined");

        /** Sent to all session participants when a player is kicked. */
        constexpr const TCHAR* GameSessionPlayerKicked = TEXT("game-session.player-kicked");

        /** Sent to all session participants when a player leaves voluntarily. */
        constexpr const TCHAR* GameSessionPlayerLeft = TEXT("game-session.player-left");

        /** Sent to players who claimed their reservation when a matchmade session */
        constexpr const TCHAR* GameSessionSessionCancelled = TEXT("game-session.session-cancelled");

        /** Sent to all session participants when the session state changes. */
        constexpr const TCHAR* GameSessionStateChanged = TEXT("game-session.state-changed");

        /** Sent when game state changes that all players should see. */
        constexpr const TCHAR* GameSessionStateUpdated = TEXT("game-session.state-updated");

        /** Sent to sessions observing a container owner when one of their containers */
        constexpr const TCHAR* InventoryContainerFull = TEXT("inventory.container.full");

        /** Sent to sessions observing a container owner when an item is placed, */
        constexpr const TCHAR* InventoryItemChanged = TEXT("inventory.item.changed");

        /** Sent to sessions observing either the source or target container owner */
        constexpr const TCHAR* InventoryItemTransferred = TEXT("inventory.item.transferred");

        /** Sent to sessions observing a location when an entity arrives at or */
        constexpr const TCHAR* LocationPresenceChanged = TEXT("location.presence-changed");

        /** Sent to sessions observing a location when location properties change */
        constexpr const TCHAR* LocationUpdated = TEXT("location.updated");

        /** Sent when matchmaking is cancelled for any reason. */
        constexpr const TCHAR* MatchmakingCancelled = TEXT("matchmaking.cancelled");

        /** Sent to all match participants when all players have accepted. */
        constexpr const TCHAR* MatchmakingMatchConfirmed = TEXT("matchmaking.match-confirmed");

        /** Sent to all match participants when someone declines. */
        constexpr const TCHAR* MatchmakingMatchDeclined = TEXT("matchmaking.match-declined");

        /** Sent to all matched players when a match is formed. */
        constexpr const TCHAR* MatchmakingMatchFound = TEXT("matchmaking.match-found");

        /** Sent to all match participants when a player accepts. */
        constexpr const TCHAR* MatchmakingPlayerAccepted = TEXT("matchmaking.player-accepted");

        /** Sent to the player when they successfully join a matchmaking queue. */
        constexpr const TCHAR* MatchmakingQueueJoined = TEXT("matchmaking.queue.joined");

        /** Periodic status update sent to players in queue. */
        constexpr const TCHAR* MatchmakingStatusUpdate = TEXT("matchmaking.status-update");

        /** Internal event carrying compiled capabilities from Permission to Connect. */
        constexpr const TCHAR* PermissionSessionCapabilities = TEXT("permission.session-capabilities");

        /** Published by services to create or update a session shortcut. */
        constexpr const TCHAR* SessionShortcutPublished = TEXT("session.shortcut-published");

        /** Published by services to remove shortcuts. */
        constexpr const TCHAR* SessionShortcutRevoked = TEXT("session.shortcut-revoked");

        /** Sent to sessions observing an entity when a status effect is granted, */
        constexpr const TCHAR* StatusEffectChanged = TEXT("status.effect-changed");

        /** Sent to all sessions for the affected account when a subscription */
        constexpr const TCHAR* SubscriptionStatusChanged = TEXT("subscription.status-changed");

        /** Generic error notification sent to client. */
        constexpr const TCHAR* SystemError = TEXT("system.error");

        /** Generic notification event for system-level messages. */
        constexpr const TCHAR* SystemNotification = TEXT("system.notification");

        /** A connection's operational status changed -- pushed to sessions in the affected realm */
        constexpr const TCHAR* TransitConnectionStatusChanged = TEXT("transit.connection.status-changed");

        /** A discoverable connection was revealed -- pushed to the discovering entity's session */
        constexpr const TCHAR* TransitDiscoveryRevealed = TEXT("transit.discovery.revealed");

        /** Journey state changed -- pushed to the traveling entity's bound session */
        constexpr const TCHAR* TransitJourneyUpdated = TEXT("transit.journey.updated");

        /** Sent to all room participants when someone requests broadcast consent. Each participant should respond via /voice/room/broadcast/consent. */
        constexpr const TCHAR* VoiceBroadcastConsentRequest = TEXT("voice.broadcast.consent-request");

        /** Sent to all room participants when the broadcast consent state changes (someone consented, someone declined, broadcast started, broadcast stopped). */
        constexpr const TCHAR* VoiceBroadcastConsentUpdate = TEXT("voice.broadcast.consent-update");

        /** Sent to existing room participants when a new peer joins. */
        constexpr const TCHAR* VoicePeerJoined = TEXT("voice.peer.joined");

        /** Sent to remaining room participants when a peer leaves. */
        constexpr const TCHAR* VoicePeerLeft = TEXT("voice.peer.left");

        /** Sent when a peer updates their SIP endpoint (e.g., ICE candidate change). */
        constexpr const TCHAR* VoicePeerUpdated = TEXT("voice.peer.updated");

        /** Sent to all room participants when the voice room is closed. */
        constexpr const TCHAR* VoiceRoomClosed = TEXT("voice.room.closed");

        /** Sent to a client when they join a voice room. */
        constexpr const TCHAR* VoiceRoomState = TEXT("voice.room.state");

        /** Sent to all room participants when the voice tier upgrades. */
        constexpr const TCHAR* VoiceTierUpgrade = TEXT("voice.tier-upgrade");

        /** Published on period-changed boundaries, ratio changes, admin clock advancement, and on-demand via TriggerTimeSync. Contains a full game time snapshot for client time rendering and interpolation. Routed via Entity Session Registry. */
        constexpr const TCHAR* WorldstateTimeSync = TEXT("worldstate.time-sync");

    } // namespace Events
} // namespace Bannou
