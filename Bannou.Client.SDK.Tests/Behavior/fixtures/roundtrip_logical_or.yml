version: '2.0'
metadata:
  id: logical-or-test
context:
  variables:
    a: { default: 0 }
    b: { default: 0 }
flows:
  main:
    actions:
      - global: { variable: result, value: 'a > 0 || b > 0 ? 1 : 0' }
